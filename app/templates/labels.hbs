<div class="column">
  <div>
    {{input type="text" value="labelName"}}
    <button class="btn" {{action 'createLabel'}}>Add label</button>
  </div>
  {{#each model as |label|}}
    {{#link-to 'labels/label/artists' label}}
      {{label.name}}
    {{/link-to}}
  {{/each}}
</div>
{{outlet}}
